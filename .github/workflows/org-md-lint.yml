name: Markdown Lint

permissions:
  contents: write
  pull-requests: write

on:
  pull_request:
    paths:
      - "**.md"
    branches:
      - "**"
  workflow_call:

jobs:
  check-markdown:
    uses: Coalfire-CF/Actions/.github/workflows/org-markdown-lint.yml@f6f14772b623150ffc6ca409b623003a8770f103 # v0.11.2
